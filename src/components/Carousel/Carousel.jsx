/* import React, { useRef, useState } from 'react'

import Pages from '../../router/Pages/Pages'
import Slide from './Slide'
import chapterActions from '../../store/chapters/actions'
import styles from "./Carousel.module.css"
import { useDispatch } from 'react-redux'

const { getChapterDetails } = chapterActions

export default function Carousel(singlePage) {
    const prev = () => {
        console.log(prev);
    }

    const next = () => {
        console.log(next);
    }
  return (
    <div className={styles.slideContainer}>
        <Slide page={singlePage}/>
    <a className='prev' onClick={prev}>AAAAA</a>
    <a className='next' onClick={next}>BBBBB</a>
    </div>
  )
} */